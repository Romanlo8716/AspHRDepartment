@model IEnumerable<Laba1.Models.MedicalBook>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>Данные медицинской книги</h1>
<input type="hidden" name="WorkerId" value="@ViewBag.WorkerId" />
<div class="form-group">
    <h4>
        <label style="width:700px">Сотрудник: @ViewBag.Surname @ViewBag.Name @ViewBag.Middlename</label>
    </h4>
</div>
  @if (User.IsInRole("admin"))
                {
<p>
    <a asp-action="AddMedicalBook" asp-route-id="@ViewBag.WorkerId">Добавить новые данные</a>
    </p>
}
else
{
    
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.placeExam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dateExam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.conclusion)
            </th>
         
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.placeExam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dateExam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.conclusion)
            </td>
                @if (User.IsInRole("admin"))
                {
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Изменить</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Подробнее</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
                    </td>
                }
                else
                {
                    
                }
        </tr>
}
    </tbody>
</table>
